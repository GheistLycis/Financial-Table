import { ScrollDetectionDirective } from './scroll-detection.directive';

describe('ScrollDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
